# Hard term-level assignments. Unlike preferences, these rules remove every
# non-matching instructor from the solver candidate set and are validated as
# hard violations in imported or manually adjusted schedules.

[[required_instructors]]
course = "MATH 4123"
instructor = "Limperis, Thomas G."
